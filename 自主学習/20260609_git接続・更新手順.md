
## 2回目以降の手順
毎回忘れる

### 1. リポジトリのフォルダに移動
```bash
cd リポジトリ名
```

### 2. 最新の状態に更新（念のため）
```bash
git pull
```

### 3. アップロードしたいフォルダをそこに置く
エクスプローラーやFinderで、リポジトリのフォルダ内にコピーするだけでOK。

### 4. あとはこの3コマンドだけ
```bash
git add .
git commit -m "コメント（何を追加したか）"
git push
```
