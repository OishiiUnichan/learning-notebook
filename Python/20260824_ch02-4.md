Chapter 2-4 ユーザーから入力を得る方法

［p.052］★ ユーザーに値を入力してもらおう
Java：
System.out.println("入力ガイド");
String 変数 = new.java.util.Scanner(System.in).nextLine();
Python：
変数 = input("入力ガイド> ")

［スクリプト］hello-input.py

［p.053］☆ 数値を入力する方法を学ぼう
［スクリプト］inch-tom-cm-input-ng.py
［注］input()関数で得た入力値は、文字列型である。
入力値を演算するためには、数字から数値に変換が必要（[p.029]int()/float()関数）

［p.054］
［スクリプト］inch-to-cm-input.py

［p.054］ ★ 演算を行うときは型を合わせることが必要
［メモ］
数値演算：互いの値は、数値同士
文字列演算：互いの値は、文字列同士
※自動型変換は生じない。変換関数できちんと変換する必要あり

［メモ］
演算は型を合わせる必要があるが、print()関数で数値に説明を加えることや、
format()メソッドで値を埋め込む方法の場合は、型は混在していても問題ない
［p.038、p.048］

※変数の型を調べる方法：type（対象の値）関数を使用